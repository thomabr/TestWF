name: "cisleep"
on:
  workflow_dispatch:
  deployment:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world! This is the pr
        # run: exit 1
  important:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run:  sleep 500
